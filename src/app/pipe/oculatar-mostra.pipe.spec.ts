import { OculatarMostraPipe } from './oculatar-mostra.pipe';

describe('OculatarMostraPipe', () => {
  it('create an instance', () => {
    const pipe = new OculatarMostraPipe();
    expect(pipe).toBeTruthy();
  });
});
